# Copy thành .env ở thư mục gốc repo (docker compose tự đọc file .env cùng cấp) — KHÔNG commit.
# Chỉ cần cho lệnh `docker compose -f docker-compose.yml -f docker-compose.migrate.yml ...`.

WP_SQL_DUMP_PATH=/Users/mac/Downloads/sql_khomanguon_c.sql
WP_PICTURE_DIR=/Users/mac/Project/Self/khomanguon-v2/picture-wp
